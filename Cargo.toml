[package]
name = "harp"
version = "0.1.0"
edition = "2024"

[dependencies]
indicatif = "0.18.1"
log = "0.4"

# 将来の実装用に残してあるが現在未使用:
# libloading = "0.8.9"  # C-like backendでの動的ライブラリロード用
# tempfile = "3.23.0"   # 一時ファイル生成用

[target.'cfg(target_os = "macos")'.dependencies]
metal = "0.32.0"
objc = "0.2"

[dev-dependencies]
env_logger = "0.11"
serial_test = "3.2"
