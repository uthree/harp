# 計算グラフ

## GraphNode
### lowering時の戦略をノードに持たせたい。
- SIMDでのベクトル化単位
- 各軸をグループとスレッドどちらで展開するか？
- View操作を組み合わせることで、新たな軸を追加してアンローリング的なことができるので、その責務はグラフ最適化処理に押し付けたい。

## 読み書きの制限
Rust的なメモリ管理をしたい。
メモリ競合を避けるため、不変(readonly)参照を複数持つか、一つだけの可変参照(書き込み可能)を持つことができる。
必要な場合はCopyノードを使って不変参照を可変として複製する。