# 計算グラフ
テンソル（多次元配列）単位での演算をDAGで表現する。

## View
Viewは、各軸の添え字からメモリオフセットに変換する写像を表現します。
この仕組みにより、ほぼゼロコストのView操作によって転置などが実現可能です。

## GraphNode
### lowering時の戦略をノードに持たせたい。
- SIMDでのベクトル化単位
- 各軸をグループとスレッドどちらで展開するか？
- View操作を組み合わせることで、新たな軸を追加してアンローリング的なことができるので、その責務はグラフ最適化処理に押し付けたい。
- WMMA(Tensor Core)をどうやって使うか？