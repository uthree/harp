[package]
name = "harp-viz"
version = "0.1.0"
edition = "2021"

[dependencies]
# GUIフレームワーク
egui = "0.30"
eframe = { version = "0.30", default-features = true }

# グラフ可視化
egui-snarl = "0.6"

# ユーティリティ
log = "0.4"

# harpへの参照
harp = { path = "../.." }

[dev-dependencies]
env_logger = "0.11"

[features]
default = []
