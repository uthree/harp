[package]
name = "eclat-backend-openmp"
version = "0.1.0"
edition = "2021"
description = "OpenMP backend for Eclat with CPU parallel execution"

[dependencies]
eclat = { path = "../.." }
log = { version = "0.4", features = ["std"] }
libloading = "0.8"
tempfile = "3"
cc = "1"
ctor = "0.2"
