[package]
name = "eclat-cli"
version = "0.1.0"
edition = "2024"
description = "CLI tools for Eclat transpiler"

[[bin]]
name = "eclat-transpile"
path = "src/main.rs"

[dependencies]
eclat = { path = "../.." }
eclat-dsl = { path = "../dsl" }
clap = { version = "4", features = ["derive"] }
