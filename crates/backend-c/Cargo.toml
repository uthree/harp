[package]
name = "eclat-backend-c"
version = "0.1.0"
edition = "2024"
description = "C backend for Eclat (code generation only)"

[dependencies]
eclat = { path = "../.." }
log = { version = "0.4", features = ["std"] }
