[package]
name = "eclat-backend-metal"
version = "0.1.0"
edition = "2024"
description = "Metal backend for Eclat (macOS only)"

[target.'cfg(target_os = "macos")'.dependencies]
eclat = { path = "../.." }
metal = "0.29"
objc = "0.2"
log = { version = "0.4", features = ["std"] }
