[package]
name = "harp-backend-metal"
version = "0.1.0"
edition = "2024"
description = "Metal backend for Harp computation graphs (macOS only)"

[target.'cfg(target_os = "macos")'.dependencies]
harp-core = { path = "../core" }
metal = "0.29"
objc = "0.2"
log = "0.4"
